# App artifacts
 /_build
/db
/cover
 /deps
/*.ez

# Generated on crash by the VM
 erl_crash.dump
*.ez

# The config/prod.secret.exs file by default contains sensitive
# data and you should not commit it into version control.
#
# Alternatively, you may comment the line below and commit the
# secrets file as long as you replace its contents by environment
# variables.
/config/prod.secret.exs

# Atom's RemoteSync plugin config file
.remotesync.json

# Ignore generated assets by webpack
priv/static/js/*.chunk.js
priv/static/js/common.js
priv/static/js/core.js
priv/static/js/polyfill.js
priv/static/js/star_wars.js

priv/static/css/core.css
priv/static/css/common.css
priv/static/css/star_wars.css

# We have example files for dockercompose.yml so let's ignore it
dockercompose.yml
